# GenOffice v0.6.66

微信 PDF 多轮 AI 审稿：

- 微信发送单份 PDF 后，附件会加密下载并保存在本机三天 Markdown 日记的资源目录。
- 系统先询问稿件类型、期刊/会议/标书级别和报告语言；选项与桌面 AI 审稿 UI 的 Science、Nature、Cell、Elsevier、IEEE、基金及标书档位一致。
- 用户回复例如“4 中文”或“6 英文”后，才启动真正的多轮 ZenMux 流程：分段证据提取、3 名委员独立审稿、1 名主席综合裁决。
- 每一次已完成的模型调用都会持久化；网络中断后从断点续跑，避免整份报告从头重复生成。
- 完整审稿报告按微信限制稳定分段发送，并写回同一篇 Markdown；原始 PDF 始终保留在本地。
- 支持回复“取消审稿”停止任务；没有 ZenMux Key 或 PDF 缺少可提取文字时会给出明确提示。
- PDF 下载仅允许微信官方 CDN、拒绝重定向、限制为 50 MB，并在 AES 解密后校验真实 PDF 文件头。

AI 审稿依赖网络和模型服务。扫描型 PDF 若没有文字层，需要先进行 OCR；公式、图片和图表若没有被 PDF 文字层表达，仍需人工复核。
