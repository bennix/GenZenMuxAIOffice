# GenOffice v0.6.64

## 微信图片日记与可靠回复

- 微信官方 ClawBot 接收层新增图片消息解析、AES-128 解密和官方 CDN 安全下载。
- 图片保存到对应三天窗口 Markdown 的同级资源目录，并以相对路径嵌入正文；日记与资源目录可整体移动。
- 仅发送图片时不逐张打断：累计 5 张后统一交给当前 ZenMux 多模态模型分析。
- 不足 5 张时，随后发送文字问题即可立即将现有图片与问题一起交给 AI。
- 每次最多向 AI 提供 5 张图片；图片正文和 AI 回复均保存在同一份本地日记中。
- 新增持久化回复发件箱、稳定微信 client ID、消息去重标记和最近消息处理记录。网络发送失败后只补发原回复，不重复写 Markdown，也不重复调用 AI。

## 安全与验证

- 图片媒体只允许从微信官方固定 HTTPS CDN 下载，拒绝跳转，单张加密内容限制为 25 MB。
- 待分析图片路径在发送 ZenMux 前必须位于用户选择的微信日记目录内。
- 新增图片批次、文字触发、iLink 图片字段、AES 密钥编码和图片格式识别测试。
- AI 功能依赖网络；微信服务、ZenMux、模型供应方或代理状态可能影响回复时间，待发送回复会保留并自动重试。
