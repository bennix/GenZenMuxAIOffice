/** Ordered numeric roles shared by UI controls, AI prompts and MCP discovery. */
export const orderedChartFields: Record<string, string[]> = {
  contour: ['Y 坐标', '高度 Z'],
  crosstab: ['列分类（计数）'],
  pivot: ['求和数值'],
  timeline: ['事件日期 / 时间'],
  gantt: ['开始日期 / 起点', '结束日期 / 终点'],
  progress: ['已完成值', '目标值'],
  bullet: ['实际值', '目标值', '区间一上限', '区间二上限', '量程上限'],
  gauge: ['当前值', '最小值', '最大值'],
  residual: ['实际值', '预测值'],
  qq: ['横轴样本', '纵轴样本'],
  'kaplan-meier': ['观测时间', '事件状态（1 事件 / 0 删失）'],
  roc: ['真实标签（0/1）', '预测分数'],
  forest: ['估计值', '区间下限', '区间上限'],
  candlestick: ['开盘', '收盘', '最低', '最高'],
  ohlc: ['开盘', '收盘', '最低', '最高'],
  bubble: ['纵轴', '气泡面积'],
  dumbbell: ['起始值', '结束值'],
  slope: ['起始值', '结束值'],
  'population-pyramid': ['左侧人群', '右侧人群'],
}

export const chartBindingNotes: Record<string, string> = {
  contour: 'x 为数值 X 坐标，y 依次为 Y 坐标和高度 Z；每个 X/Y 组合恰好一行，形成完整矩形网格，各轴至少两个坐标，共 4–4096 点。允许非等间距坐标；重复、缺失坐标或缺失高度报错，不自动插补散点。范围内取 5 个等间距等值级，用 marching squares 和双线性鞍点判别连接，边上线性插值，不外推。所有高度相同时没有可区分等高线。',
  'circle-packing': 'x 为唯一节点 ID，parent 为父节点列，y 为一个非负权重列。父节点留空自动汇总，提供的父权重须等于子节点合计。叶子圆面积与权重成正比，父圆面积包含布局空隙，不代表父权重比例；零权重节点不占面积。最多 300 个节点、12 层，至少一个正权重叶子。小圆省略放不下的文字，悬停显示完整路径和权重。',
  horizon: 'x 为唯一的数值时间或 YYYY-MM-DD 日期，2–2000 个时间点；y 为 1–6 个同量纲系列。时间排序后线性连接，空值断开。以所有系列最大绝对值的三分之一为统一带宽，将正负幅度各分三层向上折叠；正数蓝色、负数橙色，深色表示更大幅度。以零为基线，不减均值、不归一化数值。重复时间须先明确汇总方式。悬停显示原始值或线性插值，不是折叠高度。',
  hexbin: 'x 和一个 y 为完整的数值坐标，每行一个观测。将两轴数据范围分别映射到 2×1 的归一化平面，以半径 0.04 的六边形网格计数；颜色是观测条数，不是密度估计或数值总和。重复点逐条计数，空箱留白；固定分箱不随画布尺寸变化，不同轴单位不会直接决定箱形。常量轴扩展范围；不接受缺失值。',
  dendrogram:
    'x 为 2–80 个非空唯一的样本名称，y 为 1–20 个完整数值特征。使用欧氏距离和平均连接法（UPGMA）进行凝聚层次聚类；纵轴是实际合并距离。不会自动标准化，量纲不同时应先标准化数据；不接受缺失值。不需要父节点字段，同距离按原始样本顺序确定合并。',
  'word-cloud':
    'x 为预先分词后的词语，y 为一个完整非负词频列，不自动分词或推断词频。重复词语（去掉首尾空白后）求和，零频词不显示；最多 80 个不同词语。字号采用 16 + k × sqrt(词频/最大词频)，k 在 0–48 间按画布面积统一调整，不表示精确面积占比。保持水平文字及至少 16px 字号，空间不足时报错，不截断或丢弃词语。',
  'force-network':
    'x 为源节点，target 为目标节点，y 为一个完整非负边权列。使用确定性力导向布局，距离不表示业务数值，布局的吸引力不按权重调整。箭头表示方向，线宽与权重成正比；重复有向边求和，零权重边不画线但保留节点。最多 80 节点、500 条汇总边，暂不支持自连接。可缩放、拖动节点。',
  arc: 'x 为源节点，target 为目标节点，y 为一个完整非负边权列。重复有向边求和；节点按首次出现排列。上弧为左到右，下弧为右到左，圆环表示自连接，线宽与权重成正比；零权重不画线但保留节点。最多 30 节点、200 条汇总边。',
  table:
    'x 与 y 为展示字段，允许文本、数值与空值。按原始顺序显示全部选中记录，不做汇总。尺寸不足时拒绝生成以避免缩小字体或截断内容。',
  crosstab:
    'x 为行分类，y 为一个列分类字段，按记录计数，显示行列合计。空分类保留；字符串带引号以区别数值。分类顺序按首次出现排列。',
  pivot:
    'x 为行分类，target 为列分类，y 为一个求和数值字段。重复组合求和，空数值忽略，空分类保留，未出现组合为 0，显示行列合计。当前聚合方式为求和。',
  timeline:
    'x 为事件名称，y 为完整的 YYYY-MM-DD 日期列或同单位数值时间列。每个事件单独一行，横向距离表示真实时间间隔；保留原表事件顺序，最多 100 个事件。',
  gantt:
    'x 为任务名称，y 依次为开始和结束。两列均使用 YYYY-MM-DD 日期文本，或均使用同单位的数值时间，不可混用。最多 100 个任务，结束不得早于开始；零时长显示为里程碑圆点。日期按 UTC 民用日期处理。',
  beeswarm:
    'y 中每列是一组数值样本，空值忽略，最多 6 组、600 个样本单元格。仅在分组轴方向避让，保持数值坐标不变；密集时缩小点半径。x 不参与统计。',
  pictogram:
    'x 为类别，y 为 0–100 的完整整数数量，最多 20 行。每个人形图标代表 1，图标大小一致；适用于人数等离散计数，不自动舍入小数。',
  'stem-leaf':
    'y 为一个整数样本列，忽略空值，保留重复值。个位为叶，其余为茎；负数使用带负号的茎，例如 −0 | 3 = −3。最多 500 个样本、40 个茎。x 不参与计算。',
  matrix:
    'x 为非空唯一的行类别，y 为同量纲数值字段，各字段对应一列。最多 50 行、20 列，以统一色阶显示原始数值，缺失单元格留空。不计算相关系数或自动归一化。',
  radar:
    'x 为 3–40 个指标，y 为各比较对象的完整非负数值列。所有指标必须同量纲或预先采用同一评分标准；各轴共用从 0 到全局最大值的量程，不按轴自动归一化。',
  sparkline:
    'x 为按原表顺序排列的期次，y 为单个数值系列。紧凑趋势线隐藏坐标轴和图例，保留缺失值断点；纵轴按数据范围缩放，不强制从零开始。',
  'text-heatmap':
    'x 为非空唯一的词语、主题或文本类别，y 为一个或多个同量纲数值字段。最多 50 行、20 个字段。使用原始数值和统一色阶，不进行文本情感推断或自动归一化；缺失值留空，零值保留。',
  'dot-matrix':
    'x 为类别，y 为一个完整的 0–100 整数数量列，最多 20 行。每个等大圆点代表 1，每行最多 10 点；零数量只显示类别与数字，不做舍入或汇总。',
  'proportional-area':
    'x 为类别，y 为一个完整非负数值列，最多 40 行且至少一个正数。圆面积与原始值成正比，半径按平方根缩放；零值保留文字但不画圆。各圆按表格顺序排列，不做汇总。',
  icicle:
    'x 为唯一节点 ID，parent 为父节点列，y 为非负权重列。根的 parent 为空，父节点留空则自动汇总；提供的父权重须等于子权重之和。宽度表示权重，向下表示层级，零权重节点不占面积，整体权重须为正。',
  stream:
    'x 为连续数值或 YYYY-MM-DD 日期文本，y 为 2–20 个同量纲非负系列，所有单元格必须完整。重复时间点按系列求和并排序；至少两个不同时间点。层的垂直厚度表示数量，浮动基线不表示零值，不按百分比归一化。',
  'theme-river':
    'x 为 YYYY-MM-DD 日期文本，y 为 2–20 个主题的非负数量列，所有单元格必须完整。重复日期按主题求和并排序；至少两个不同日期。厚度表示主题数量，浮动位置不表示数值大小。',
  calendar:
    'x 为 YYYY-MM-DD 日期文本（不接受 Excel 日期序列号），y 选择一个数值列。同日数值求和，空值不计入；无数据日期留空，0 仍为有效数值。最多显示连续三年，各年共用色阶。',
  kpi: '每行一个已计算指标，x 为名称，y 为一个完整数值列，最多 12 行。不自动求和、平均或推断增长率；直接显示原始数值。',
  progress:
    'x 为指标名称，y 为完整的已完成值、目标值。完成值非负，目标严格为正。允许超过 100%，按实际比例显示并扩展坐标轴，100% 标线表示目标。',
  pyramid:
    'x 为层名称，y 为一个完整非负数值列且合计为正。保留从顶到底的原表顺序，每层面积按数值占比分配；层高不直接代表数值。',
  marimekko:
    'x 为类别，y 为各组成部分的完整非负数值列。类别总量决定列宽，类别内比例决定高度，矩形面积代表整体占比。总量须为正，零总量类别不占宽度。',
  'small-multiples':
    'x 为共同类别顺序，y 选择 2–6 个同量纲数值系列；每列一个柱状小图，所有小图共用数值范围，空值保留为空值。',
  'scatter-matrix':
    'y 选择 2–6 个数值字段，每行一个观测，各小图使用成对非空观测。同一字段共用轴范围，对角线显示字段与自身的关系。x 列不参与计算。',
  bullet:
    '每行一个指标，五列完整非负数值。三段区间上限递增，实际和目标不超过量程。蓝条为实际，黑竖线为目标，深到浅灰色为三个评价区间；所有行共用数值尺度。',
  gauge:
    '仅一行数据，x 为指标名称；y 依次为当前值、最小值、最大值，必须完整。最小值 < 最大值且当前值在量程内。不要自动假定百分比或 0–100 量程。',
  waffle:
    'x 为类别，y 为一个完整非负数值列且合计为正。100 个方格采用最大余数法舍入，每格约 1%；小占比可能没有方格，图例保留实际百分比。',
  adjacency:
    'x 为源节点、target 为目标节点、y 为一个完整非负边权列。重复边求和，允许自环与循环。行是源，列是目标，无连接显示 0；最多 200 个节点。',
  control:
    '单值控制图（I 图），y 选择一个至少三个完整观测的数值列，x 为按原表顺序的期次。使用相邻移动极差估计 σ=MR均值/1.128，界限为均值±3σ。界限不是业务规格限；只标记越界点，不判定其他运行规则。',
  jitter:
    'y 中每列为一组样本，忽略空值。只在分组轴上确定性错开位置，不改变样本值；点仍可能重叠。x 列不参与统计。',
  residual:
    '残差 = 实际值 − 预测值，横轴为预测值，忽略不完整配对。x 列不参与计算。不自动拟合模型。',
  qq: '双样本 QQ 图，每列至少两个样本，空值独立忽略。线性插值计算最多 512 个共同分位点，虚线为 y=x；不是正态性检验。x 列不参与计算。',
  'kaplan-meier':
    '每行一名受试者。时间非负，事件为 0/1，数据完整。x 不参与计算。右删失；同刻事件先于删失。当前只绘制总体曲线，无置信带。',
  roc: '每行一个观测，标签为 0/1，分数越大越倾向正类。必须同时有正负类且数据完整。x 不参与计算。',
  forest:
    '每行一个研究或指标。下限 ≤ 估计值 ≤ 上限，数据完整。使用提供的区间，不推断置信水平、权重或汇总效应。',
  candlestick: 'x 为有序日期或期次。最低 ≤ 开盘/收盘 ≤ 最高，四列完整。保留原始行顺序。',
  ohlc: 'x 为有序日期或期次。最低 ≤ 开盘/收盘 ≤ 最高，四列完整。保留原始行顺序。',
  bubble:
    'x 必须数值。面积非负且至少一个正数；圆面积按数值比例缩放，0 面积不显示。忽略不完整观测。',
  quadrant: 'x 为数值，y 为一个数值列。横纵轴以 0 为分界，使用对称轴范围。',
  'population-pyramid': 'x 为年龄段。两组完整非负数值使用同一尺度，至少一个正数。',
  violin: 'y 中每列是一组样本，忽略空值；每组至少两个样本。共同密度宽度尺度。x 不参与统计。',
  ridgeline: 'y 中每列是一组样本，忽略空值；每组至少两个样本。共同密度宽度尺度。x 不参与统计。',
  histogram: 'y 只选一个样本数值列，忽略空值。x 不参与统计。',
  density: 'y 只选一个样本数值列，忽略空值。x 不参与统计。',
  boxplot: 'y 中每列是一组样本，忽略空值；使用 Tukey 四分位和离群值。x 不参与统计。',
  treemap:
    'x 为唯一节点 ID，parent 为父节点列，y 为一个非负权重列。根的 parent 为空；父合计如提供须等于子节点之和。',
  sunburst:
    'x 为唯一节点 ID，parent 为父节点列，y 为一个非负权重列。根的 parent 为空；父合计如提供须等于子节点之和。',
  tree: 'x 为唯一节点 ID，parent 为父节点列，y 为一个非负权重列。根的 parent 为空，不允许循环或缺失父节点。',
  sankey: 'x 为源节点，target 为目标节点，y 为一个完整非负流量列。流向必须无环。',
  waterfall: 'x 为依次发生的变化类别，y 为一个完整增减值列；从 0 累积。不要把合计行当变化输入。',
  correlation: 'y 至少两个数值列，使用成对完整观测计算 Pearson 相关。x 不参与统计。',
  parallel: 'y 至少两个数值维度，每行一个观测。只使用完整观测。',
}

export function chartBindingInfo(chartId: string) {
  return {
    ...(orderedChartFields[chartId] ? { orderedY: orderedChartFields[chartId] } : {}),
    ...(chartBindingNotes[chartId] ? { notes: chartBindingNotes[chartId] } : {}),
  }
}
