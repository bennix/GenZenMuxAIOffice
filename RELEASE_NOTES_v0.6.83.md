# ZenOffice v0.6.83

本版本将 Word AI 生图工作台的用户界面品牌统一为 **ArtFlow**。

## 品牌更新

- Word“插入 → AI 生图”的工作台标题、对话框标签、提示文案和图片说明统一使用 ArtFlow。
- ArtFlow 内的来源归因说明同步更新，不再向用户显示旧名称。
- Landing Page、README、自动更新版本和跨平台下载入口更新到 v0.6.83。

## 兼容性

- 保留既有本地项目、画布、设置和素材存储键，升级后原有创作数据可继续使用。
- ArtFlow 继续复用 ZenOffice 的 ZenMux 设置，并支持将生成图片直接插入 Word。

> AI 功能依赖 ZenMux 与相关网络服务，响应速度和可用性可能受当前网络状态影响。
