## 主要更新

- Markdown 新增“微信公众号排版”：可选择主题和内容密度，实时预览并复制内联样式 HTML，同时写入纯文本剪贴板备用内容。
- Mermaid 工作室升级为 Pretty Mermaid 与编辑级图表两种模式，提供 6 类常用模板、15 套主题和出版级图表起始结构；ZenMux 可生成或修改标准 Mermaid 源码。
- 设置页允许保存自定义 OpenAI-compatible Base URL；默认仍为 `https://zenmux.ai/api/v1`，API Key 继续只在本机持久保存并于界面遮罩显示。
- AI 流式请求统一读取当前网关配置，改进自定义端点的 URL 解析、错误传播和重试兼容性。
- 公众号富文本、AI 提问与回复共享可靠的 HTML + 纯文本剪贴板写入机制。
- PDF 文字编辑新增功能区字体、字号、粗体与斜体控件，并优先保持原文字的字体、颜色、背景和布局。
- PDF 内置 Noto 与 Liberation 字体回退资源；原字体不可用或仅嵌入子集时，可按简体中文、繁体中文、日文、韩文和拉丁文字选择相近字体并嵌入最小字体子集，保证保存文件在其他应用中可见。
- 首页、首次启动与设置界面进一步移除旧云账号路径，聚焦本地文件和 ZenMux / OpenAI-compatible 网关配置。

## 验证

- 全项目格式检查、类型检查和生产构建通过。
- PDF 27 个测试文件 / 367 项测试通过，包括字体包发现、字符覆盖、字体子集嵌入和保存回读。
- Markdown 9 个测试文件 / 153 项测试通过，包括微信富文本导出、Mermaid 主题、AI 生成和源码编辑。
- AI Provider 93 项、共享 UI 22 项测试通过。
- macOS DMG 使用 Developer ID 签名并完成 Apple 公证与 stapling。
- Windows 安装包暂未做 Authenticode 签名，SmartScreen 可能提示未知发布者。

AI 功能依赖网络，网络、代理或供应方响应状态可能影响完成时间。
