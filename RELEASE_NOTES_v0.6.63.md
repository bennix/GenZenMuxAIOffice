## 主要更新

- 设置页新增“微信日记”：可通过微信 8.0.70+ 官方 ClawBot 流程扫码绑定，在 GenOffice 运行期间接收微信文字。
- 收到的内容按“三天窗口”写入本机 Markdown 日记；凌晨 4 点前归入前一天，文件按年份整理，目录可由用户选择。
- 可选用设置中当前 ZenMux / OpenAI-compatible AI 自动回复，提问与回复会追加到同一篇 Markdown，形成可继续编辑的移动端日记。
- 新增 `记：…`（只写入）、`撤回`、`结束`、`帮助` 和探活命令，并可从设置页直接打开当前日记。
- 微信绑定凭据使用操作系统安全存储加密，本地配置文件权限限制为当前用户；所有携带令牌的网络请求仅允许微信官方 HTTPS 主机，拒绝不可信跳转地址。
- 改进消息游标提交顺序：一批消息全部写入和处理成功后才推进游标，降低应用中途退出导致日记内容丢失的风险。
- Landing Page、README、安装链接及 Windows / Ubuntu DEB / Linux RPM 构建工作流统一更新为 v0.6.63。

## 验证

- 全项目格式检查、Lint 与 TypeScript 类型检查通过（仅保留既有 React Hooks 警告，无错误）。
- Shell 14 个测试文件 / 152 项测试通过，新增三天窗口、凌晨 4 点换日、Markdown 追加/撤回、命令分类与微信官方主机安全校验测试。
- 全项目生产构建通过。
- macOS DMG 使用 Developer ID 签名并完成 Apple 公证与 stapling。
- Windows 安装包暂未做 Authenticode 签名，SmartScreen 可能提示未知发布者。

微信日记仅在 GenOffice 运行且监听已启用时接收内容。AI 功能依赖网络，网络、代理、微信服务或模型供应方响应状态可能影响完成时间。
