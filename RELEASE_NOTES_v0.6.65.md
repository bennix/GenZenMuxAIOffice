# GenOffice v0.6.65

微信 AI 回复可靠性热修复：

- 修复微信接口返回 HTTP 200 但业务错误时被误判为发送成功的问题。
- 回复改为微信确认送达后再写入 Markdown，避免“日记已有 AI 内容、微信却没有回复”。
- 持久化微信发送阶段；网络中断时重试原回复，不重复调用 AI 或重复写入日记。
- AI 长回复按微信单条消息限制自动分段，并为各段保留稳定的幂等消息 ID。
- 登录失效会明确提示重新绑定微信，不再静默吞掉发送错误。

AI 功能依赖网络，网络、代理或微信服务状态仍可能影响响应速度；失败的回复会保留在本地待发送队列中。
