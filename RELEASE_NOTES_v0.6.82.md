# ZenOffice v0.6.82

本版本把 LoveArt 创作工作台与 GB/T 9704 公文能力接入 Word，并改善应用内更新检查。

## 新功能

- Word“插入 → AI 生图”内嵌 LoveArt，支持参考图、风格模板、画布创作、图片编辑和一键插入文档。
- Word“设计 → 公文排版”支持普通材料、正式发文、信函、命令（令）和纪要，并可生成兼容 Office、WPS 与 LibreOffice 的 DOCX。
- 公文面板新增 AI 起草、AI 润色和 AI 检查，建议稿经用户采用后才写入正文。
- 打开“设置 → 关于”会自动检查更新并显示当前状态；支持的平台继续使用自动下载与安装流程。

## 质量与兼容性

- 公文生成不依赖系统 `zip` 命令，可在 macOS、Windows 与 Linux 使用。
- 公文结构校验覆盖 A4 版心、字体字号、标题层级、红头模式及单双页页码。
- LoveArt 使用独立本地存储，并复用 ZenOffice 的 ZenMux 设置；API Key 不会在子模块重复持久化。

> AI 功能依赖 ZenMux 与相关网络服务，响应速度和可用性可能受当前网络状态影响。
